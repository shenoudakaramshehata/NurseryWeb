@page
@model Nursery.Areas.Admin.Pages.AgeCategories.IndexModel
@{
}
<div class="page-content">
    <div class="container-fluid">

        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h2 class="card-header d-flex justify-content-between align-items-center">
                            @sharedResource["AgeCategories"]
                        </h2>
                    </div>
                    <div class="card-body">
                        @(Html.DevExtreme().DataGrid<Nursery.Models.AgeCategory>()
.DataSource(ds => ds.Mvc()
.Controller("AgeCategories")
.LoadAction("Get")
.InsertAction("Post")
.UpdateAction("Put")
.DeleteAction("Delete")
.Key("AgeCategoryId")
)
.RemoteOperations(true)
.Columns(columns => {
    columns.AddFor(m => m.AgeCategoryTlAr).Caption(sharedResource["TlAr"]);
    columns.AddFor(m => m.AgeCategoryTlEn).Caption(sharedResource["TlEn"]);
    columns.AddFor(m => m.from).Caption(sharedResource["From"]);
    columns.AddFor(m => m.to).Caption(sharedResource["To"]);
    columns.AddFor(m => m.IsActive).Caption(sharedResource["IsActive"]);

}

)

.Editing(e => e.Mode(GridEditMode.Popup)
.Texts(c => c.CancelRowChanges(sharedResource["Cancel"]) .SaveRowChanges(sharedResource["Save"]) .EditRow(sharedResource["Edit"]).AddRow(sharedResource["Add"]).ConfirmDeleteMessage(sharedResource["Are you sure to delete this record ?"]))




.AllowUpdating(true)
.AllowAdding(true)
.AllowDeleting(true)
.UseIcons(true)


.Popup(p => p
.Title(sharedResource["AgeCategory"])
.ShowTitle(true)
.MaxWidth(750)
.RtlEnabled(bool.Parse(sharedResource["ArLang"]))


.Height(290)).Form(f => f.Items(items =>
{
    items.AddGroup()
    .ColCount(2)
    .ColSpan(2)
    .Items(groupItems =>
    {
        groupItems.AddSimpleFor(e=>e.AgeCategoryTlAr).DataField(sharedResource["AgeCategoryTlAr"]).IsRequired(true);
        groupItems.AddSimpleFor(e=>e.AgeCategoryTlEn).DataField(sharedResource["AgeCategoryTlEn"]).IsRequired(true);
        groupItems.AddSimpleFor(e=>e.from).DataField(sharedResource["from"]).IsRequired(true);
        groupItems.AddSimpleFor(e=>e.to).DataField(sharedResource["to"]).IsRequired(true);
        groupItems.AddSimpleFor(e=>e.IsActive);

    });



}))

)
    .Paging(paging => paging.PageSize(10))
                        .Pager(pager =>
                        {
                            pager.Visible(true);
                            pager.DisplayMode(GridPagerDisplayMode.Full);
                            pager.ShowPageSizeSelector(true);
                            pager.AllowedPageSizes(new JS("[5, 10, 'all']"));
                            pager.ShowInfo(true);
                            pager.ShowNavigationButtons(true);

                        })
        .HeaderFilter(headerFilter => headerFilter.Visible(true))
                            .ShowRowLines(true).ShowColumnLines(true)
                            .RowAlternationEnabled(true)
                            .AllowColumnReordering(true)
                            .ShowColumnLines(true)
                            .ShowBorders(true)
                            .SearchPanel(searchPanel => searchPanel
                            .Visible(true)
                            .Width(240)
                            .Placeholder(sharedResource["SearchTxt"]))
                            .RemoteOperations(true)
                            .RtlEnabled(bool.Parse(sharedResource["ArLang"]))
)

                    </div>
                </div>
            </div>
        </div>
    </div>
</div>